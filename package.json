{
  "name": "eggbot",
  "version": "0.1.0",
  "description": "Autonomous multi-agent AI assistant",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "reset": "rm -f ~/.eggbot/eggbot.db ~/.eggbot/brain-index.db && echo 'Reset: sessions + brain index cleared. Brain vault kept.'",
    "reset:hard": "rm -rf ~/.eggbot/eggbot.db ~/.eggbot/brain-index.db ~/.eggbot/brain ~/.eggbot/logs && echo 'Hard reset: all data cleared.'",
    "kill": "launchctl unload ~/Library/LaunchAgents/ai.eggbot.plist 2>/dev/null; lsof -ti :4444 | xargs kill -9 2>/dev/null; sleep 1; true",
    "restart": "npm run kill && npm run dev"
  },
  "dependencies": {
    "@fastify/static": "^8.1.1",
    "@fastify/websocket": "^11.0.2",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "better-sqlite3": "^11.9.1",
    "fastify": "^5.2.1",
    "grammy": "^1.41.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.19.15",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
